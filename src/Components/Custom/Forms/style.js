const style = () => {
  const textContainer = {
    width: "100%",
    my: "2%",
  };
  const text = {
    color: "error",
    width: "100%",
  };

  const buttonContainer = {
    width: "100%",
    marginTop: "2%",
  };

  const button = {
    color: "#FF7B54",
    width: "100%",
    height: "100%",
  };
  return { textContainer, text, buttonContainer, button };
};
export default style;
